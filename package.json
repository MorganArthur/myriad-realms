{
  "name": "myriad-realms",
  "version": "0.8.0",
  "private": true,
  "description": "万象之境：浏览器端像素世界模拟器",
  "scripts": {
    "check": "node --check engine-core.js && node --check game.js && node --test",
    "test": "node --test"
  }
}
